---
interface Props {
  name: string;
  headline: string;
}

const { name, headline } = Astro.props;
---

<section class="max-w-3xl mx-auto px-4 py-20">
  <h1 class="text-5xl font-bold tracking-tight mb-4">{name}</h1>
  <p class="text-xl text-gray-600">{headline}</p>
</section>
