---
import { getCollection } from 'astro:content';
import ArticleLayout from '../../layouts/ArticleLayout.astro';
import BLineAside from '../../components/BLineAside.astro';
import BLineImage from '../../components/BLineImage.astro';
import BLinePanel from '../../components/BLinePanel.astro';
import BLinePanelTrigger from '../../components/BLinePanelTrigger.astro';

export async function getStaticPaths() {
  const essays = await getCollection('writing', ({ data }) => data.type === 'essay');
  return essays.map((entry) => ({
    params: { slug: entry.slug },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const { Content } = await entry.render();
const { data } = entry;

const mdxComponents = { BLineAside, BLineImage, BLinePanel, BLinePanelTrigger };
---

<ArticleLayout
  title={data.title}
  description={data.description}
  date={data.date}
  video_desktop={data.video_desktop}
  video_mobile={data.video_mobile}
  poster_desktop={data.poster_desktop}
  poster_mobile={data.poster_mobile}
  focus_desktop={data.focus_desktop}
  focus_mobile={data.focus_mobile}
  title_appears_at={data.title_appears_at}
  freeze_at={data.freeze_at}
>
  <Content components={mdxComponents} />
</ArticleLayout>
